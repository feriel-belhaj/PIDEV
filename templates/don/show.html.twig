{% extends 'baseBack.html.twig' %}

{% block title %}Don{% endblock %}

{% block body %}
    <h1>Don</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ don.id }}</td>
            </tr>
            <tr>
                <th>Donationdate</th>
                <td>{{ don.donationdate ? don.donationdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ don.amount }}</td>
            </tr>
            <tr>
                <th>Paymentref</th>
                <td>{{ don.paymentref }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ don.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_don_index') }}">back to list</a>

   <!--  <a href="{{ path('app_don_edit', {'id': don.id}) }}">edit</a> -->

  <!--  {{ include('don/_delete_form.html.twig') }} -->
{% endblock %}
