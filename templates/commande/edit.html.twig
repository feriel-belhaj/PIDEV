{% extends 'base.html.twig' %}

{% block title %}Edit Commande{% endblock %}

{% block body %}
    <h1>Edit Commande</h1>

    {{ include('commande/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_commande_index') }}">back to list</a>

<<<<<<< Updated upstream
    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
=======
                    {% if form.quantites[loop.index0].vars.errors|length > 0 %}
                        <div class="text-danger">
                            {% for error in form.quantites[loop.index0].vars.errors %}
                                <p>{{ error.message }}</p>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>
            {% endfor %}

            <button type="submit" class="btn btn-primary border-0 rounded-pill py-3 px-5 me-3 animated bounceInLeft" style="font-family: 'Open Sans', sans-serif;">
                Modifier
            </button>

            {{ form_end(form) }}
        </div>
    </div>

    <style>
        .text-start label {
            font-weight: bold;
        }
    </style>
{% endblock %}
>>>>>>> Stashed changes
